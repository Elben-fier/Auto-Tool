# Auto-Tool — Vobile 内容审核自动化工具集

针对 **Vobile 内容审核平台**（`tools.vobile.cn`）日常审核作业开发的一套 Tampermonkey 油猴脚本 + 本地 Python 检测后端，覆盖「**短剧审核批量判错**」与「**短视频审核勾选 / 采集 / 删除**」两大场景，把重复性人工操作变成规则化的自动批处理。

> 所有图片与文本检测**均在本机完成**（回环地址 `127.0.0.1:5002`），不上传任何外部服务器。

---

## ✨ 核心能力

| 能力 | 说明 |
|---|---|
| **智能判错（短剧审核）** | 三级判定：终极关键词 → 关键词双列比对 / 相似度闸门 → 匹配分与子样本分档，自动决定「直接全剧审核」还是「全选 + 批量错误 + 全剧审核」 |
| **状态码闸门** | 批量错误后**必须等全部子样本状态码变 594**（且页面转圈结束）才点全剧审核，杜绝"系统还在处理脚本就点下一步" |
| **严格等待加载** | 所有点击前 / 展开后 / 提交后都等 antd 转圈消失；转圈期间不做完成判定 |
| **容错与自愈** | 等待 30s / 60s 各自动重试点击一次；仍无响应则**永久跳过该合集并继续下一行**；问题合集自动收起 |
| **人像检测（本地）** | YOLOv8n(person) + YuNet(人脸兜底)：人脸 / 背影 / 侧脸 / 全身都算"有人"；手机、屏幕（电视/显示器/笔记本）单独识别 |
| **图片相似度（本地）** | SSIM + dHash(8/16) + HSV 直方图 + 宽高比 加权比对，用于"第5列 vs 第6列"一致性校验 |
| **一键勾选（短视频）** | 一页一轮：错误行勾选→批量错误、其余勾正确→批量正确，逐页自动循环 |
| **可观测** | 面板实时状态与进度、逐行判错原因、TSV 明细/跳过清单一键复制到剪贴板 |

---

## 🏗 系统架构

```
┌─────────────────────────── 浏览器（Edge + Tampermonkey） ───────────────────────────┐
│  dramaReview* 页面                            qualityTest* 页面                     │
│  ├─ 短剧审核9.0.user.js（判错全流程）+ 平台筛选自动全选.user.js                        │
│  └─ 短视频审核-一页勾选版.user.js ／ 短剧审核-标签版v2.2-YOLO.user.js 等               │
└───────────────────────────────┬────────────────────────────────────────────────────┘
                                │ HTTP（仅回环 127.0.0.1:5002）
┌───────────────────────────────▼────────────────────────────────────────────────────┐
│  本地 Python 后端（backend/，Flask）                                                 │
│  ├─ /detect_batch_detail  YOLOv8n(person/tv/laptop/cell phone) + YuNet 人脸           │
│  ├─ /compare              SSIM + dHash + 颜色直方图 + 宽高比                          │
│  ├─ /detect, /detect_batch, /health, /debug_screenshot                               │
│  └─ TaskQueue：单线程串行 + 按图片张数限速（4.5 张/秒，超限 429）                      │
└──────────────────────────────────────────────────────────────────────────────────────┘
```

---

## 📁 目录结构

```
Auto-Tool/
├── README.md                          # 本文件：项目总览
├── .gitignore
├── backend/                           # 本地检测后端（Python）
│   ├── README.md                      #   依赖、模型准备、启动、阈值
│   ├── server.py                      #   Flask 路由
│   ├── detector.py                    #   人像检测 / 图片相似度算法
│   ├── task_queue.py                  #   单线程限速队列
│   ├── requirements.txt
│   └── run.bat
├── docs/
│   ├── 部署与使用指南.md               #   环境准备 → 启动 → 安装 → 调参 → 排错
│   ├── 后端接口与算法说明.md            #   接口契约、算法原理、实测数据
│   ├── 版本演进记录.md                 #   各脚本版本里程碑与设计取舍
│   ├── 项目需求说明.md                 #   需求规格（整理版）
│   ├── 修改方向与需求记录.md            #   需求变更记录
│   ├── 项目需求说明-原始.txt
│   └── 修改方向-原始.txt
├── 短剧审核-批量判错/                  # 目标页面 dramaReview*
│   ├── README.md
│   ├── 短剧审核9.0.user.js             #   ⭐ 当前推荐（终极关键词版）
│   ├── 平台筛选自动全选.user.js
│   └── 历史版本/                       #   短剧审核 1.0 ~ 8.0
└── 短视频审核-勾选采集/                # 目标页面 qualityTest*
    ├── README.md
    ├── 短视频审核-一页勾选版.user.js     #   ⭐ 当前推荐（一页一轮自动审核）
    ├── 短剧审核-标签版v2.2-YOLO.user.js  #   人脸提取 + 批量判错删除（双标签）
    ├── 低分key_id采集.user.js
    ├── 无人脸ID提取.user.js
    ├── 自动全选批量删除.user.js
    └── 历史版本/
```

---

## 🚀 快速开始

```bash
# 1) 后端：装依赖 + 放模型（模型获取见 backend/README.md）+ 启动
cd backend
pip install -r requirements.txt
python server.py          # 或双击 run.bat

# 2) 浏览器：Tampermonkey 导入对应 .user.js（同页面只启用一个脚本）
# 3) 打开审核页面，右下角出现控制面板，点「开始」
```

- 详细步骤与调参：**[docs/部署与使用指南.md](docs/部署与使用指南.md)**
- 接口与算法：**[docs/后端接口与算法说明.md](docs/后端接口与算法说明.md)**
- 版本演进与判定规则：**[docs/版本演进记录.md](docs/版本演进记录.md)**

---

## 🧰 工具清单

### 短剧审核（`dramaReview*`）

| 脚本 | 版本 | 说明 | 需后端 |
|---|---|:---:|:---:|
| [短剧审核9.0](短剧审核-批量判错/短剧审核9.0.user.js) | 9.0.0 | ⭐ 终极关键词 + 双列比对 + 相似度闸门 + 匹配分分档；严格等待加载 / 30s·60s 重试 / 永久跳过 | 否 |
| [平台筛选自动全选](短剧审核-批量判错/平台筛选自动全选.user.js) | 1.0.0 | 自动重置并滚动点选平台多选框的全部选项 | 否 |
| 历史版本 1.0 ~ 8.0 | — | 见 [该目录 README](短剧审核-批量判错/README.md) | 否 |

### 短视频审核（`qualityTest*`）

| 脚本 | 版本 | 说明 | 需后端 |
|---|---|:---:|:---:|
| [短视频审核-一页勾选版](短视频审核-勾选采集/短视频审核-一页勾选版.user.js) | 1.0.0 | ⭐ 一页一轮：判定 → 错误批量错误 / 正确批量正确 → 等下一页刷新 | ✅ |
| [短剧审核-标签版v2.2-YOLO](短视频审核-勾选采集/短剧审核-标签版v2.2-YOLO.user.js) | 2.2.0 | 浮窗双标签：人脸识别提取 key_id + 批量判错删除 | ✅ |
| [低分key_id采集](短视频审核-勾选采集/低分key_id采集.user.js) | 1.4.3 | 得分 ≤ 阈值 或 名称含关键词 → 采集 key_id | 否 |
| [无人脸ID提取](短视频审核-勾选采集/无人脸ID提取.user.js) | 1.1.3 | 批量人脸检测 → 提取无人脸/低分/关键词 key_id | ✅ |
| [自动全选批量删除](短视频审核-勾选采集/自动全选批量删除.user.js) | 1.0.0 | 全选 → 批量删除 → 循环直到清空 | 否 |

> 标记 ✅ 的脚本需要 `backend/` 提供的本地服务（默认 `127.0.0.1:5002`）。

---

## ⚠️ 注意事项

1. **不可逆操作**：脚本会真实执行「批量错误 / 批量正确 / 全剧审核 / 批量删除」，请先在少量数据上验证规则与阈值；
2. **一页一脚本**：同一页面不要同时启用多个脚本；
3. **窗口约束**：依赖屏幕坐标截屏的功能（`/detect`）要求浏览器窗口不最小化、目标区域不被遮挡，建议缩放 100%；
4. **数据安全**：图片与文本检测全部在本机完成，无数据外发；
5. **识别率**：人像/人脸模型对极小目标、严重遮挡、强模糊仍有漏检可能，可用面板阈值与 `detector.py` 常量微调。
